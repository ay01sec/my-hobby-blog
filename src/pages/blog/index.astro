---
import BaseLayout from "../../layouts/BaseLayout.astro"
const posts = await Astro.glob("./*.mdx")
---
<BaseLayout title="Blog | 記事一覧">
<h1>Blog</h1>
<ul>
{posts.map((p) => (
<li><a href={p.url}>{p.frontmatter.title ?? p.url}</a></li>
))}
</ul>
</BaseLayout>